// const map = require('unist-util-map')
// import { map } from 'unist-util-map';

export default ({ markdownAST }, pluginOptions) => {

  // const processChildren = (ast) => {
  //   if(ast.children) {

  //   }

  // };

  // if(markdownAST.type === 'root') console.log(markdownAST);

  // const processedMarkdownAST = 



  // process.exit();

  // var next = map(tree, function(node) {
  //   return node0;
  //   // return node.type === 'leaf'
  //   //   ? Object.assign({}, node, {value: 'CHANGED'})
  //   //   : node
  // })

  console.log(next);
  process.exit();

  return markdownAST;
}


